# Context Hub — 知識抽出レポート (2026-04-10)

## 抽出された知識

| # | カテゴリ | 要約 | 詳細 | 信頼度 | 反映先セクション |
|---|---------|------|------|--------|----------------|
| 1 | technical_insight | Claude Max 100 はレートリミットを解消するが日次コストが大幅増加する | Max 100 なしでは集中利用時にすぐレートリミットに達する。Max 100 利用時は実測で日次 ~$100 の消費が確認されており、CLAUDE.md に記載の日次アラート閾値 $10 を大幅に超える。環境変数 `OTEL_CC_INSIGHT_DAILY_COST_ALERT` を実態に合わせて調整が必要。 | medium | otel-cc メトリクス解釈ガイド（予算前提） |
| 2 | known_issue | Claude Max 100 なしの環境では重い利用で即座にレートリミット到達 | 複数セッション（5114395b, a3cf7e96）で言及。Max 100 申請前は率限により作業が中断した経験あり。otel-cc の監視対象ユーザーのプラン差異を考慮する必要がある。 | medium | otel-cc メトリクス解釈ガイド（既知の問題） |

**除外した項目とその理由:**
- AI社会変容への考察 → プロジェクト固有でない哲学的考察
- AWS Security Hub / Guard Duty → lifeai プロジェクト外
- メール基盤・DNS/SPF/DKIM/DMARC → 外部プロジェクトの実装作業
- SQS→Go worker パターン → 外部プロジェクトの知見
- 会議でのポジショニング手法 → 一般的なソフトスキル
- ANS/Claude 資格学習 → 個人学習計画

---

## CLAUDE.md への変更パッチ

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -182,7 +182,12 @@ Clean Architecture の依存方向に従い、内側から外側へ実装する:

 ### 指標の良否判断基準

-> **予算前提:** 月上限 $200（日次目標 $6.67、日次アラート $10）
+> **予算前提（API プラン）:** 月上限 $200（日次目標 $6.67、日次アラート $10）
+>
+> **Claude Max 100 利用時の注意:** Max 100 プランはレートリミットが発生しないため使用量が増加する傾向がある。実測値として日次 ~$100 に達することが確認されており、上記閾値をそのまま適用すると常にアラート状態となる。Max 100 利用者は `OTEL_CC_INSIGHT_DAILY_COST_ALERT` を実態に合わせて引き上げること（例: `50.0` 〜 `100.0`）。

 | 指標 | 良好 | 要注意 | 問題 |
```

**# reason:** Claude Max 100 利用時の日次コスト実態（~$100/day）が CLAUDE.md の予算前提（$10 アラート）と大きく乖離しており、otel-cc を使うユーザーが混乱する可能性がある。2セッションで繰り返し言及された事実として記録する。
算で日次 $100 程度になることがある。この場合、コスト指標はトークン消費量の**相対的な増減トレンド**を見る用途に限定し、絶対値でのアラート判断は適用しない。

# reason: Max 100プランの実際の消費実態(~$100/day)がCLAUDE.mdの閾値($10)と大幅乖離しており、otel-ccのアラート解釈を誤らないよう注釈が必要
```
