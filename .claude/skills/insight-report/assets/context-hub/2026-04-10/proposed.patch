--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -182,7 +182,12 @@ Clean Architecture の依存方向に従い、内側から外側へ実装する:

 ### 指標の良否判断基準

-> **予算前提:** 月上限 $200（日次目標 $6.67、日次アラート $10）
+> **予算前提（API プラン）:** 月上限 $200（日次目標 $6.67、日次アラート $10）
+>
+> **Claude Max 100 利用時の注意:** Max 100 プランはレートリミットが発生しないため使用量が増加する傾向がある。実測値として日次 ~$100 に達することが確認されており、上記閾値をそのまま適用すると常にアラート状態となる。Max 100 利用者は `OTEL_CC_INSIGHT_DAILY_COST_ALERT` を実態に合わせて引き上げること（例: `50.0` 〜 `100.0`）。

 | 指標 | 良好 | 要注意 | 問題 |
