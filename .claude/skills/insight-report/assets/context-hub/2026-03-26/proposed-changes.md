# Context Hub — 知識抽出レポート (2026-03-26)

## 抽出された知識

| # | カテゴリ | 要約 | 詳細 | 信頼度 | 反映先セクション |
|---|---------|------|------|--------|----------------|
| — | — | 該当なし | — | — | — |

## 除外理由

対話ログ2件を分析した結果、抽出対象となる知識はありませんでした。

1. **`cd` でハイフン始まりディレクトリに移動する方法** — 一般的なシェル知識であり、プロジェクト固有ではない（除外パターン1）
2. **Claude Code が `~/.claude/projects/<project-slug>/*.jsonl` に自動書き込みする件** — 既に CLAUDE.md の otel-cc データフローセクションに `~/.claude/projects/*.jsonl` として記載済み（除外パターン5）

## CLAUDE.md への追記提案

追記すべき内容はありません。
