## 基本情報

| 項目 | 記入欄 |
|---|---|
| **エントリー番号** | （事務局記入） |
| **タイトル** | Claude Code 使用状況モニタリング基盤「otel-cc」 |
| **応募者名** | 佐藤 亮介 |
| **エントリー形態** | 個人 |
| **所属** | 製造ビジネステクノロジー部 |

---

## 応募カテゴリ

- [x] **A. 効率化部門** — 自分/チームの業務改善
- [ ] **B. サービス部門** — 顧客向けに提供可能なもの
- [ ] **C. ナレッジ部門** — プロンプト集、手順書、ノウハウ

---

## 概要

Claude Codeの使用ログを自動解析しGrafanaで可視化するモニタリング基盤。コスト・キャッシュ率・エラーをリアルタイム監視し、データに基づく運用改善を実現。

---

## 課題と解決策

### 解決したい課題

- Claude Code は API 従量課金・Max 定額プランいずれでも、「どれだけトークンを消費したか」「キャッシュが効いているか」を確認する標準手段がなかった
- 使用状況が見えないため、非効率な運用（セッション毎回新規作成、ツールエラーの繰り返し）に気づけず放置していた
- Opus と Sonnet のどちらを使うべきかの判断材料がなく、感覚でモデルを選択していた
- チームで Claude Code を使う際、メンバー間の使い方のばらつきを把握できなかった
- 異常なコスト発生（長時間セッションの暴走等）に事後でしか気づけなかった

### 解決策（やったこと）

Claude Code を使って Rust 製のモニタリングバイナリ「otel-cc」を開発した。

- ログ自動解析: `~/.claude/projects/*.jsonl` を inotify でリアルタイム監視し、差分スキャンで新規行のみを処理
- データパイプライン: 解析結果を SQLite に蓄積 → Prometheus がスクレイプ → Grafana で可視化
- 7つの専用ダッシュボード:
  - Cost Management: 日別コスト推移、予算消化率、プロジェクト別内訳
  - Session Efficiency: キャッシュヒット率、圧縮イベント頻度
  - Tool Analytics: ツール別エラー率ランキング、アンチパターン検出
  - Model Optimization: モデル別コスト効率比較
  - Anomaly Detection: コスト・エラー率の±2σ逸脱検知
  - Periodic Review: 期間比較、7日ローリング平均
  - Claude Code Monitor: チーム全体の利用状況一覧
- 自動インサイト: 5分周期でメトリクスを分析し、閾値超過時に Grafana アノテーションを自動投稿
- チーム対応: `$user` テンプレート変数で個人別/チーム全体の表示切り替え

### 使用ツール・技術

- Claude Code Max プラン（otel-cc の開発に使用）
- Rust（tokio, axum, rusqlite, notify, tracing）
- SQLite（データストア、差分スキャン状態管理）
- Prometheus（メトリクス収集、15秒スクレイプ）
- Grafana（可視化、7ダッシュボード自動プロビジョニング）
- Docker Compose（インフラ構成管理）

---

## 効果

### 定量効果

※ いずれも導入前後の見込み値

| 指標 | Before | After | 効果 |
|---|---|---|---|
| 使用状況の把握 | 確認手段なし（状況が見えず放置） | ダッシュボードで常時可視化、月2〜3回の改善アクション実施 | 見込み月3時間削減（非効率の早期発見・是正） |
| キャッシュヒット率の改善 | 確認手段なし（推定60〜80%で運用） | 可視化により`--resume`運用を徹底（99%以上へ） | トークン消費量 見込み20〜40%削減 |
| モデル選択の最適化 | 全作業をOpusで実行（$14.67/セッション） | モデル別コスト可視化でタスク別に使い分け（Sonnet: $4.55/セッション） | 見込み$10/セッション削減（切替対象セッション） |
| 異常コストの検知 | 月末の請求書で事後に気づく | ±2σ逸脱を自動検知→Grafanaアノテーション通知 | 見込み月2時間削減（原因調査・事後対応の短縮） |
| チームの利用状況把握 | 個人ごとにヒアリング（30分/回×月1回） | ダッシュボードで一覧確認（5分） | 見込み月25分削減/チーム |

### 定性効果

- コストが可視化されたことで「使いすぎかも」という漠然とした不安がなくなり、Claude Code を躊躇なく使えるようになった
- 「キャッシュ率が下がっている→セッション継続を徹底しよう」など、数値に基づく改善サイクルが回るようになった
- ダッシュボードを見てモデルの使い分け（設計=Opus、実装=Sonnet）を判断できるようになった
- ±2σ の異常検知で、コスト急増やエラー率スパイクを事前に察知できるようになった

---

## 対象人数・利用頻度

| 項目 | 記入欄 |
|---|---|
| **対象人数** | 現在 1 名（自分）。全社展開時は約 100 名が対象 |
| **利用頻度** | 毎日（ダッシュボードは常時稼働、データは15秒間隔で更新） |
| **展開状況** | 自分のみ（チーム用 Docker Compose 構成でいつでも展開可能） |

---

## 再現性・展開可能性

### 他の人が使うために必要なこと

- Docker（Docker Compose が動く環境）
- Claude Code（ログが `~/.claude/projects/` に出力されること）
- 導入手順:
  1. リポジトリをクローン
  2. `docker compose up otel-cc prometheus grafana`（設定不要）
  3. ブラウザで `http://localhost:3000` を開く
- 所要時間: 約5分、特別なスキル不要

### 他部署・他業務への応用可能性

- 全社展開: 部署を問わず Claude Code を使う人全員に適用可能
- チーム管理: チーム用 Docker Compose 構成で共有サーバーに一元化し、メンバーは settings.json に3行追記するだけ
- 応用例:
  - チームリーダーがメンバーの AI 活用状況を把握し、使い方のガイダンスに活用
  - 経営層向けに全社の AI 投資対効果を可視化するレポートとして活用
  - 月次予算管理: ダッシュボード上で予算設定→残額・消化率を自動計算

---

## デモ・エビデンス（推奨）

### デモ関連

- Grafana ダッシュボード操作のデモ動画を添付

### スクリーンショット

- 添付: Grafana ダッシュボード（Cost Management / Session Efficiency / Anomaly Detection）

### 実績データ

- 2026年3月17日〜29日の13日間で 32セッション の使用データを自動収集・分析
- otel-cc 実測値: キャッシュヒット率 99.98%、ツールエラー率 2.6%、ツール呼び出し 1,845回
- 開発実績: Rust 7,137行、152テスト（カバレッジ約90%）、63コミット

---

## 補足資料（任意）

- ソースコード一式（添付）
- アーキテクチャ設計書（添付）
- Grafana ダッシュボード定義ファイル（7種、JSON 形式、添付）
